Drop the audio files here:

- `spinning.wav` — plays once when the reels start spinning.
- `thunk.wav` — plays each time a reel comes to a complete stop (layers/overlaps).

Served at `/public/sounds/spinning.wav` and `/public/sounds/thunk.wav`.
